// Importa a biblioteca React.
import React from "react";

const UserContext = React.createContext(); // O createContext é usado para criar um contexto, que é um objeto que contém um valor que pode ser acessado por outros componentes.

export default UserContext; // Está exportando o componente UserContext.
