// Importa a biblioteca React.
import React from "react";

// Criado um componente chamado NotFound.
const NotFound = () => {
  // Retorna uma div com um título e um parágrafo.
  return (
    <div>
      <h2>Not found</h2>
      <p>Essa página não existe ou não foi encontrada.</p>
    </div>
  );
};

export default NotFound; // Está exportando o componente NotFound.
